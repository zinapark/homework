{
  "scripts": {
    "start": "live-server . --port=4000 --host=localhost --no-browser",
    "format": "prettier . --check",
    "webp": "simpe-webp-converter"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "simple-webp-converter": "^1.0.3"
  }
}
